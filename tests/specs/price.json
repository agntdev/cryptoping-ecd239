[
  {
    "name": "/price",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "/price \u003cticker\u003e fetches current price for a single ticker; /price alone returns current prices for the user's watchlist"
            }
          }
        ],
        "send": {
          "text": "/price"
        }
      }
    ]
  }
]
