[
  {
    "name": "Settings",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Open settings to set timezone, quiet hours, morning summary time, and per-user defaults"
            }
          }
        ],
        "send": {
          "callback": "user:settings"
        }
      }
    ]
  }
]
